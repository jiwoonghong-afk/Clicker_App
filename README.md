# Switch Clicker

A browser-based mechanical keyboard clicker game with switch modes, keycap skins, combo scoring, upgrades, themes, and level progression.

## Play

Open `index.html` in a browser, or host the repository with GitHub Pages.

Use `V`, `B`, `N`, and `M` to press the four keycaps. Use `1` through `6` to change switch modes.
